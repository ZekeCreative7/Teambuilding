# Lina Culture Platform Upgrade Development Plan

작성일: 2026-06-06  
기준 자료:
- `LINA_PLATFORM_OPERATING_REPORT.md`
- `LINA_PLATFORM_OPERATING_MODEL.png`
- 현재 구축 방향: Pulse Survey 신호를 조직도, 세션, 메시지, 웰니스 실행, 문화 확산 맵, 검증 루프로 연결하는 조직문화 운영 플랫폼

---

## 0. 이 문서의 목적

이 문서는 Lina Culture Platform 다음 버전을 개발할 때 Codex가 따라야 할 기획/개발 규칙이다.

이번 업그레이드의 목표는 단순 기능 추가가 아니다. 현재 플랫폼을 다음 구조로 고도화한다.

```text
입력 데이터
  Pulse Survey, 세션 설문, 조직도, 세션 기획, 메시지, 피드백, 실행 기록

데이터 정합성
  조직명 매칭, 연도별 조직 변경 추적, 문항 변경 추적, 업로드 검증

분석 엔진
  Pulse 분석, 세션 신호 분석, 조직문화 점수 체계, 문화 확산 맵

기획 보조 도구
  세션 설계, 메시지 설계, GPT 템플릿 생성, 히스토리 보드

운영 화면
  선택형 대시보드, Pulse Story 페이지, People & Organization, Session Board, Message Center

검증 루프
  실행 후 피드백, 다음 Pulse 비교, You Said -> We Did, 조직문화 변화 추적
```

핵심 원칙은 하나다.

> Lina는 조직문화 데이터를 보고서로 끝내지 않고, 변화에 유연하고 신뢰가 높은 조직으로 운영하기 위한 실행 시스템이어야 한다.

---

## 1. 제품 북극성

### 1.1 최종 목적

Lina Culture Platform은 다음 목적을 가진다.

- 변화가 필요한 조직을 조기에 발견한다.
- 변화 수용도, 신뢰도, 피로도, 로열티, 실행 상태를 통합해 조직문화 상태를 판단한다.
- 피로도가 높은 조직에는 웰니스와 회복 개입을 연결한다.
- 경영진과 조직에 대한 신뢰를 높이는 메시지와 세션을 설계한다.
- 실행한 개입이 실제로 조직문화에 영향을 줬는지 연도별로 검증한다.

### 1.2 제품이 피해야 할 방향

- 단순 Pulse Survey 대시보드로 끝나면 안 된다.
- 모든 기능을 한 화면에 밀어 넣으면 안 된다.
- 세션 만들기를 복잡한 저작 도구로 만들면 안 된다.
- AI가 만든 결과를 사람 승인 없이 자동 적용하면 안 된다.
- 핵심 포뮬라를 클라이언트 JavaScript 안에 그대로 노출하면 안 된다.

### 1.3 개발 우선순위

1. 데이터 구조와 매칭 규칙을 먼저 만든다.
2. 분석 엔진과 포뮬라 실행 구조를 분리한다.
3. 입력/업로드/검증 흐름을 안정화한다.
4. 대시보드와 Pulse 페이지를 모듈형으로 재설계한다.
5. 세션/메시지 도구는 기획 보조와 기록 관리 중심으로 만든다.
6. 모든 실행 결과가 다시 점수 체계와 문화 확산 맵으로 돌아오게 만든다.

---

## 2. 데이터 입력 체계

### 2.1 입력 데이터 종류

플랫폼은 다음 입력을 정식 데이터 타입으로 다룬다.

| 입력 | 주요 내용 | 필수 여부 | 저장 목적 |
|---|---|---:|---|
| 조직도 마스터 | 조직 ID, 조직명, 상위 조직, 리더, 구성원, 직급 | 필수 | 모든 분석의 기준 축 |
| Pulse Survey | 연도, 문항, 조직별 객관식 점수, 주관식 의견 | 필수 | 조직문화 기본 진단 |
| 세션 설문 | 변화 수용도, 신뢰도, 피로도, 로열티, 세션 피드백 | 선택/중요 | Pulse 사이의 추가 신호 |
| 세션 기획 | 목적, 대상, 구조, 스토리, 스크립트, 운영안 | 선택 | 실행 관리와 재사용 |
| 메시지 기획 | 의도, 대상, 핵심 메시지, 톤, 스크립트 | 선택 | 커뮤니케이션 관리 |
| 실행 기록 | 일정, 참석, 완료, 후속 액션 | 선택/중요 | 실행 검증 |
| GPT 결과 | 분석, 템플릿, 추천 액션, 메시지 초안 | 선택 | 기획 보조와 자동 구조화 |

### 2.2 조직도 기준 원칙

조직 분석의 기준은 항상 최신 조직도 데이터다.

```text
업로드 데이터의 조직명
  -> 최신 조직도 기준 조직명/조직 ID와 비교
  -> 불일치 감지
  -> 사용자에게 매칭 요청
  -> 매칭 테이블 저장
  -> 분석 엔진 실행
```

Pulse Survey나 세션 설문에 들어 있는 조직명은 분석 기준이 아니다. 조직명은 매칭 대상이고, 최종 분석 기준은 조직도에 저장된 `orgId`다.

### 2.3 조직명 변경 감지 기능

조직명이 들어간 자료가 업로드되면 다음을 자동 감지한다.

- 기존 조직도에 없는 신규 조직명
- 기존 조직명과 유사하지만 다른 표기
- 연도별 조직명 변경 가능성
- 통폐합/분리 가능성
- 조직도에는 있지만 업로드 데이터에는 없는 조직
- 업로드 데이터에는 있지만 조직도에는 없는 조직

감지 결과는 바로 사용자에게 보여준다.

```text
감지 유형
  신규 조직 후보
  이름 변경 후보
  통합 후보
  분리 후보
  제외 후보

사용자 선택
  기존 조직에 매칭
  새 조직으로 등록
  별칭으로 저장
  해당 연도에만 임시 매칭
  분석에서 제외
```

### 2.4 조직 매칭 데이터 모델

```js
orgAliasMap = {
  id: 'alias_2026_001',
  year: 2026,
  uploadedName: 'HR Innovation Div.',
  matchedOrgId: 'HR_INNOVATION',
  canonicalName: 'HR Innovation Division',
  confidence: 0.82,
  mappingType: 'renamed' | 'alias' | 'new' | 'merged' | 'split' | 'excluded',
  approvedByUser: true,
  approvedAt: '2026-06-06T00:00:00.000Z'
}
```

분석 엔진은 `approvedByUser=true`인 매칭만 사용한다. 미확정 매칭이 남아 있으면 분석 리포트 생성 버튼을 비활성화한다.

---

## 3. Pulse Survey 업그레이드 계획

### 3.1 Pulse 문항 관리 페이지

새로운 `Survey Item Manager` 페이지를 만든다.

필수 기능:

- 연도별 문항 등록
- 문항 ID 관리
- 문항 텍스트 변경 추적
- 카테고리 변경 추적
- 점수 방향 관리
- 신규/삭제/수정 문항 비교
- 문항별 분석 사용 여부 설정
- 연도별 문항 세트 복제

### 3.2 문항 데이터 모델

```js
surveyItem = {
  itemId: 'Q01',
  year: 2026,
  text: '나는 회사의 변화 방향을 이해하고 있다.',
  category: 'change_acceptance',
  scaleType: 'likert_5',
  scoreDirection: 'positive',
  active: true,
  previousItemId: 'Q01',
  changedFromPreviousYear: false,
  changeNote: ''
}
```

문항 텍스트가 바뀌면 같은 `Q01`이라도 분석 엔진은 변경 이력을 기록해야 한다. 연도 비교 시에는 동일 문항 비교인지, 유사 문항 비교인지, 비교 불가인지 구분한다.

### 3.3 Pulse 업로드와 즉시 분석 생성

Pulse 데이터 업로드 시 다음 순서로 처리한다.

```text
1. 파일 파싱
2. 연도 감지
3. 문항 세트 감지
4. 조직명 감지
5. 조직명 매칭 확인
6. 문항 변경 확인
7. 데이터 정규화
8. 분석 엔진 실행
9. 분석 보고서 버전 생성
10. 대시보드/Pulse 페이지/문화맵 갱신
```

분석 보고서는 저장 가능한 버전 객체로 만든다.

```js
analysisReport = {
  reportId,
  datasetId,
  year,
  generatedAt,
  sourceHash,
  orgMappingVersionId,
  surveyItemSetVersionId,
  engineVersion,
  summary,
  organizationSignals,
  dashboardWidgets,
  storySections,
  recommendations
}
```

### 3.4 GPT 템플릿 생성 루프

문항과 조직도를 통합해 GPT가 업로드 템플릿을 만들 수 있게 한다.

흐름:

```text
Survey Item Manager
  -> 현재 연도 문항 세트 선택
  -> 최신 조직도 선택
  -> GPT 템플릿 생성 프롬프트 만들기
  -> GPT 결과 JSON/CSV 템플릿 붙여넣기
  -> 플랫폼에서 구조 검증
  -> 템플릿 저장
  -> 이후 업로드 파일 기준으로 사용
```

GPT는 템플릿을 제안할 수 있지만, 플랫폼은 반드시 스키마 검증을 해야 한다.

---

## 4. 세션 설문과 추가 분석 데이터

### 4.1 세션 설문 목적

세션 설문은 Pulse Survey 사이의 보조 신호다. 특히 다음 항목을 직접 측정한다.

- 변화 수용도
- 경영진/리더 신뢰도
- 변화 피로도
- 조직 로열티
- 웰니스 필요도
- 세션 효과성
- 후속 지원 필요성

### 4.2 세션 설문 문항 방향

객관식 문항 예시:

| 지표 | 문항 방향 | 점수 방향 |
|---|---|---|
| 변화 수용도 | 나는 이번 변화의 필요성을 이해한다 | 높을수록 좋음 |
| 변화 수용도 | 나는 변화 과정에서 내 역할을 알고 있다 | 높을수록 좋음 |
| 신뢰도 | 나는 경영진이 변화 과정을 책임 있게 운영한다고 느낀다 | 높을수록 좋음 |
| 신뢰도 | 나는 우리 리더가 팀의 우려를 잘 전달한다고 느낀다 | 높을수록 좋음 |
| 피로도 | 최근 변화로 인해 정서적 피로가 크다 | 높을수록 위험 |
| 피로도 | 업무 외 회복 시간이 부족하다고 느낀다 | 높을수록 위험 |
| 로열티 | 나는 이 조직에서 계속 성장하고 싶다 | 높을수록 좋음 |
| 웰니스 | 현재 우리 팀에는 회복/웰니스 지원이 필요하다 | 높을수록 지원 필요 |

주관식 문항 예시:

- 변화 과정에서 가장 불안한 점은 무엇인가?
- 조직이 더 신뢰받기 위해 먼저 해야 할 행동은 무엇인가?
- 이번 세션에서 도움이 된 점과 부족했던 점은 무엇인가?
- 우리 팀의 피로도를 낮추기 위해 가장 필요한 지원은 무엇인가?

### 4.3 세션 설문 업로드

결과 업로드는 Pulse와 같은 검증 단계를 따른다.

```text
파일 업로드
  -> 세션 ID/조직명/연도 감지
  -> 조직 매칭
  -> 객관식 점수 정규화
  -> 주관식 의견 태깅
  -> 세션 효과 분석
  -> 조직별 추가 신호로 저장
```

---

## 5. 통합 점수 체계와 문화 확산 맵

### 5.1 점수 체계의 목적

모든 입력 데이터는 최종적으로 하나의 조직문화 상태 판단 체계로 연결되어야 한다.

점수 체계는 다음 질문에 답해야 한다.

- 어느 조직이 변화에 유연한가?
- 어느 조직이 피로도가 높아 지원이 필요한가?
- 어느 조직이 경영진/리더를 신뢰하지 못하는가?
- 어느 조직이 웰니스 개입의 우선 대상인가?
- 어느 조직이 변화 확산의 앵커 역할을 할 수 있는가?
- 어느 조직이 다음 Pulse에서 개선 여부를 확인해야 하는가?

### 5.2 공개 가능한 개념 지표

사용자 화면에는 다음 지표를 보여준다.

| 지표 | 의미 | 데이터 출처 |
|---|---|---|
| Change Readiness | 변화 이해, 수용, 행동 준비도 | Pulse + 세션 설문 |
| Trust Index | 경영진/리더/조직에 대한 신뢰 | Pulse + 세션 설문 |
| Fatigue Load | 변화 피로도와 회복 필요 | Pulse + 세션 설문 + 웰니스 신호 |
| Loyalty Signal | 조직에 대한 몰입과 잔류 의향 | Pulse + 세션 설문 |
| Wellness Need | 웰니스 개입 필요도 | 피로도 + 주관식 + 세션 피드백 |
| Execution Momentum | 세션/메시지/액션 실행 정도 | 실행 기록 |
| Culture Propagation Potential | 긍정 문화 확산 가능성 | 신뢰 + 수용도 + 영향력 + 실행 상태 |
| Risk & Support Need | 즉시 지원 필요성 | 피로도 + 리스크 + 낮은 신뢰 |

### 5.3 문화 확산 맵 방향

문화 확산 맵은 다음 구조를 기본으로 한다.

```text
X축: 변화 유연성 / 신뢰 / 로열티
Y축: 피로도 / 리스크 / 지원 필요
크기: 조직 규모 또는 영향력
색상: 상태 사분면
테두리/패턴: 실행 개입 여부
시간 필터: 연도/분기/세션 전후
```

사분면:

- 확산 앵커: 변화 유연성 높음, 피로도 낮음
- 지원 필요: 변화 유연성 높음, 피로도 높음
- 위험: 변화 유연성 낮음, 피로도 높음
- 관망/정체: 변화 유연성 낮음, 피로도 낮음

### 5.4 핵심 포뮬라 보안 원칙

핵심 산식과 가중치는 공개 파일에 두지 않는다.

금지:

- `platform.js`, `app.js`, `index.html` 안에 핵심 가중치 직접 작성
- 브라우저에서 다운로드 가능한 JSON에 핵심 포뮬라 저장
- Git에 핵심 포뮬라 파일 커밋

권장 구조:

```text
Client UI
  -> 분석 요청 생성
  -> Private Formula Function 호출
  -> 결과만 수신
  -> 화면/리포트에 반영

Private Formula Module
  -> 비공개 환경에 저장
  -> 핵심 가중치/로직 참조
  -> 결과 객체만 반환
```

정적 웹앱 단계에서 임시로 로컬 파일을 써야 한다면 다음 원칙을 따른다.

```text
private/
  formula-engine.local.js
  formula-config.local.json

.gitignore
  private/
  *.local.js
  *.local.json
```

단, 브라우저에서 실행되는 코드는 완전히 숨길 수 없다. 핵심 아이디어를 회사나 외부 저장소에 남기고 싶지 않다면 최종 구조는 반드시 서버 사이드 함수, 비공개 저장소, 또는 개인 소유의 별도 실행 환경으로 분리해야 한다.

---

## 6. 대시보드 업그레이드

### 6.1 대시보드 원칙

대시보드는 고정 화면이 아니라 선택형 운영 보드가 되어야 한다.

사용자는 원하는 정보를 추가/삭제할 수 있어야 한다.

```text
Dashboard Builder
  -> Widget Library
  -> Widget 선택
  -> 데이터 소스 선택
  -> 시각화 타입 선택
  -> 레이아웃 저장
```

### 6.2 위젯 라이브러리

초기 위젯 후보:

- Culture Health Summary
- Change Readiness Trend
- Trust vs Fatigue Map
- Wellness Need Priority
- Risk Organization List
- Culture Propagation Map
- Session Progress
- Message History
- You Said -> We Did
- Year-over-Year Pulse Comparison
- Intervention Impact
- Target Group Overview

### 6.3 인포메이션 디자인 툴 선택

각 위젯은 데이터 성격에 맞는 시각화를 선택할 수 있어야 한다.

| 데이터 유형 | 추천 시각화 |
|---|---|
| 연도별 변화 | 라인 차트, 스몰멀티플 |
| 조직별 비교 | 정렬 막대, 히트맵 |
| 사분면 상태 | 2D 매트릭스 |
| 우선순위 | 랭킹 리스트, 점수 카드 |
| 실행 흐름 | 타임라인, 칸반 |
| 세션 진행 | 단계형 진행 바 |
| 주관식 의견 | 테마 클러스터, 키워드 맵 |

---

## 7. Pulse Survey 페이지 업그레이드

### 7.1 페이지 목적

Pulse 페이지는 단순히 위에서 아래로 정보를 나열하지 않는다. 사용자가 다음 스토리를 따라가게 만든다.

```text
1. 올해 조직문화 상태는 어떤가?
2. 작년과 비교해 무엇이 변했는가?
3. 어떤 조직에서 신호가 강한가?
4. 왜 그런 신호가 나왔는가?
5. 어떤 행동이 필요한가?
6. 그 행동은 어떤 세션/메시지/웰니스로 이어지는가?
7. 다음에 무엇을 검증할 것인가?
```

### 7.2 Pulse Story 섹션

필수 섹션:

- Executive Signal
- Year-over-Year Movement
- Organization Pattern
- Trust & Fatigue Diagnosis
- Wellness Need
- Priority Groups
- Recommended Interventions
- Next Verification Plan

각 섹션은 사용자가 바로 실행으로 넘어갈 수 있는 액션을 가져야 한다.

예:

- `세션 만들기`
- `메시지 만들기`
- `타겟그룹 저장`
- `대시보드에 추가`
- `리포트 버전 저장`

---

## 8. 세션 만들기 업그레이드

### 8.1 세션 도구의 역할

세션 만들기는 완성형 자동 생성기가 아니다. 사용자의 고민을 고도화하는 보조 도구다.

중심 기능은 다음 세 가지다.

1. 목적 명확화
2. 아이디어 구조화
3. 스토리텔링 뼈대 설계

디테일은 사용자가 계속 편집하고 마무리할 수 있어야 한다.

### 8.2 세션 생성 흐름

```text
1. 세션 목적 선택/작성
2. 대상 조직과 현재 신호 확인
3. 핵심 질문 3개 정리
4. 세션 구조 선택
5. 스토리 흐름 만들기
6. 운영 스크립트 초안 만들기
7. 사용자가 직접 수정
8. 최종 기획안 저장
9. 세션 보드에 등록
10. 실행 후 피드백 연결
```

### 8.3 세션 보드

완성된 기획과 스크립트는 보드처럼 관리한다.

보드 상태:

- Draft
- Ready
- Scheduled
- Done
- Feedback Added
- Archived

저장 항목:

```js
sessionProject = {
  sessionId,
  title,
  targetOrgIds,
  purpose,
  coreQuestions,
  structure,
  storyFlow,
  script,
  materials,
  linkedPulseReportId,
  linkedCultureSignals,
  status,
  feedback,
  createdAt,
  updatedAt
}
```

### 8.4 세션 피드백

세션 종료 후 다음 정보를 입력할 수 있어야 한다.

- 참석자 수
- 만족도
- 변화 이해도 변화
- 신뢰도 변화
- 피로도 변화
- 가장 유용했던 점
- 남은 우려
- 후속 액션

피드백은 Culture Score와 문화 확산 맵에 반영된다.

---

## 9. 메시지 기능 업그레이드

### 9.1 메시지 도구의 역할

메시지 기능도 자동 작성기가 아니라 의도와 구조를 잡아주는 기획 도구다.

필수 입력:

- 메시지 목적
- 대상
- 현재 조직 신호
- 원하는 감정 변화
- 핵심 메시지
- 피해야 할 표현
- 전달 채널

### 9.2 메시지 생성 흐름

```text
1. 메시지 의도 정의
2. 대상 조직/그룹 선택
3. 현재 신호 확인
4. 메시지 구조 선택
5. 초안 생성
6. 사용자가 수정
7. 최종본 저장
8. 발송/공유 상태 기록
9. 이후 피드백 또는 Pulse 변화와 연결
```

### 9.3 메시지 히스토리

메시지는 검색 가능한 기록으로 남긴다.

```js
messageRecord = {
  messageId,
  title,
  targetOrgIds,
  intent,
  channel,
  outline,
  finalDraft,
  linkedSignals,
  status,
  sentAt,
  feedback,
  createdAt,
  updatedAt
}
```

---

## 10. 에이전트 기반 기획 워크플로우

이번 개발은 바로 코딩하지 않는다. 아래 에이전트 흐름으로 기획 산출물을 먼저 만든다.

### 10.1 Research Agent

목적:

- 조직문화, 변화관리, 신뢰, 피로도, 웰니스, 로열티 관련 프레임 조사
- 유사 HR/People Analytics 제품 흐름 조사
- 세션 설문 문항 후보 조사

산출물:

- 참고 프레임 목록
- 적용 가능한 평가 항목 후보
- Lina 목적에 맞지 않는 항목 제외 이유

### 10.2 Fact Check Agent

목적:

- Research Agent 결과의 근거 확인
- 과장된 주장 제거
- 실제 제품/이론/프레임과 Lina에 적용할 해석 구분

산출물:

- 검증된 항목
- 주의가 필요한 항목
- Lina 내부 가설로만 써야 하는 항목

### 10.3 Service Planning Agent

목적:

- Lina의 목적에 맞는 서비스 구조 설계
- 사용자가 실제로 쓰는 흐름 기준으로 화면과 기능 정리
- 세션/메시지/대시보드가 하나의 운영 루프로 연결되게 설계

산출물:

- 사용자 여정
- 기능 우선순위
- 화면별 역할
- MVP 범위

### 10.4 Data Structuring Agent

목적:

- 입력 데이터와 산출물 데이터 구조화
- 연도별 Pulse, 조직도 버전, 세션 설문, 메시지, 피드백을 하나의 스키마로 연결

산출물:

- 데이터 모델
- 업로드 템플릿
- JSON Schema
- Firestore 구조 제안

### 10.5 Engine Design Agent

목적:

- 통합 점수 체계 설계
- 문화 확산 맵 계산 구조 설계
- 공개 가능한 결과와 비공개 포뮬라 분리

산출물:

- 엔진 입출력 명세
- 비공개 포뮬라 파일 구조
- 결과 객체 스키마
- 테스트 케이스

### 10.6 Requirement Writer Agent

목적:

- 위 산출물을 개발 요건 기획서로 정리
- 개발 순서와 검증 기준을 단계별로 만든다.

산출물:

- 개발 요건 기획서
- 단계별 체크리스트
- 화면별 요구사항
- 데이터별 요구사항
- 테스트/검증 기준

---

## 11. 개발 단계

### Phase 1. 데이터 기반 정리

목표:

- 조직도 기준 데이터 구조 확정
- 조직명 매칭 기능 설계/구현
- Pulse 문항 관리 구조 구현

완료 기준:

- 업로드 데이터에서 조직명 불일치를 감지한다.
- 사용자가 매칭을 승인/저장할 수 있다.
- Pulse 문항을 연도별로 관리할 수 있다.
- 미확정 매칭이 있으면 분석을 막는다.

### Phase 2. 분석 리포트 엔진

목표:

- Pulse 업로드 후 즉시 분석 리포트 생성
- 연도별 보고서 버전 저장
- 문항 변경과 조직 변경을 반영한 비교 분석 기반 마련

완료 기준:

- 데이터셋 업로드 후 `analysisReport`가 생성된다.
- 보고서 버전 목록을 볼 수 있다.
- 같은 연도/다른 연도 비교가 가능하다.
- 변경된 문항은 비교 가능 여부가 표시된다.

### Phase 3. 세션 설문과 피드백

목표:

- 세션 설문 문항 세트 생성
- 세션 설문 결과 업로드
- 세션 피드백을 조직별 추가 신호로 저장

완료 기준:

- 세션별 설문 결과를 업로드할 수 있다.
- 객관식/주관식 결과가 분리 저장된다.
- 세션 피드백이 조직별 신호에 반영된다.

### Phase 4. 비공개 포뮬라 엔진

목표:

- 핵심 점수 체계와 문화 확산 맵 로직을 UI에서 분리
- 클라이언트에는 결과만 전달

완료 기준:

- UI 파일에 핵심 가중치가 없다.
- 비공개 포뮬라 파일은 Git에 포함되지 않는다.
- 엔진 입력/출력 스키마가 고정된다.
- 같은 입력에 대해 결과가 재현된다.

### Phase 5. 선택형 대시보드

목표:

- 위젯 라이브러리 기반 대시보드 구현
- 사용자가 필요한 정보를 추가/삭제

완료 기준:

- 위젯을 추가/삭제할 수 있다.
- 위젯별 데이터 소스와 시각화 타입을 선택할 수 있다.
- 레이아웃이 저장된다.
- Pulse, 세션, 메시지, 문화맵 데이터가 연결된다.

### Phase 6. Pulse Story 페이지

목표:

- Pulse 페이지를 정보 나열이 아니라 스토리형 분석 페이지로 재설계

완료 기준:

- 올해 상태, 변화, 원인, 실행, 검증이 한 흐름으로 이어진다.
- 각 섹션에서 세션/메시지/타겟그룹/대시보드 액션으로 이동할 수 있다.

### Phase 7. 세션/메시지 보드

목표:

- 세션과 메시지를 기획 보조 도구 + 기록 관리 도구로 재설계

완료 기준:

- 세션 기획안을 보드로 저장/관리한다.
- 메시지 최종본과 히스토리를 저장/관리한다.
- 세션 피드백과 메시지 반응이 조직 신호에 연결된다.

### Phase 8. 검증 루프

목표:

- 실행 결과가 다시 분석과 문화 확산 맵으로 돌아오게 한다.

완료 기준:

- You Said -> We Did 형태의 실행 추적이 가능하다.
- 실행 전후 신호 비교가 가능하다.
- 다음 Pulse와 연결된 개선/악화 분석이 가능하다.

---

## 12. 파일/구조 제안

현재 정적 앱 구조를 유지하되, 다음과 같이 역할을 분리한다.

```text
/data
  survey-items.json
  org-alias-map.json
  upload-templates.json

/schemas
  pulse-dataset.schema.json
  survey-item.schema.json
  session-survey.schema.json
  analysis-report.schema.json
  dashboard-widget.schema.json

/engines
  public-analysis-adapter.js
  report-builder.js
  dashboard-widget-engine.js

/private
  formula-engine.local.js
  formula-config.local.json

/pages
  survey-item-manager.js
  dashboard-builder.js
  pulse-story.js
  session-board.js
  message-board.js

/docs
  requirement-spec.md
  data-model.md
  formula-engine-contract.md
```

`/private`는 반드시 `.gitignore`에 포함한다.

---

## 13. Codex 개발 규칙

이 프로젝트를 개발할 때 Codex는 다음 규칙을 따른다.

1. 개발 전에 이 문서를 먼저 확인한다.
2. 현재 사용자에게 실제로 열린 파일/앱이 무엇인지 확인한다.
3. 조직도 데이터는 항상 최신 기준 데이터로 취급한다.
4. 조직명이 들어간 업로드는 바로 분석하지 않고 매칭 검증을 먼저 한다.
5. Pulse 문항은 연도별 버전으로 관리한다.
6. 문항이 바뀌면 연도 비교 가능 여부를 표시한다.
7. 세션/메시지 도구는 복잡한 생성기가 아니라 기획 보조 도구로 만든다.
8. 대시보드는 고정 화면이 아니라 위젯 조합형으로 만든다.
9. Pulse 페이지는 스토리 흐름을 가져야 한다.
10. 핵심 포뮬라와 가중치는 공개 클라이언트 파일에 넣지 않는다.
11. AI 결과는 사람이 검토/승인한 뒤 적용한다.
12. 모든 실행 데이터는 나중에 검증 가능하도록 기록한다.
13. 분석 결과는 반드시 조직도, 세션, 메시지, 문화 확산 맵 중 하나 이상의 실행 화면으로 연결한다.
14. 사용자가 “되는지 확인”을 요청하면 실제 UI에서 검증한다.
15. 기능 추가 후에는 데이터 저장/불러오기/재계산 흐름까지 확인한다.

---

## 14. MVP 범위

가장 먼저 만들 MVP는 다음이다.

1. 조직명 매칭/승인 기능
2. Pulse 문항 연도별 관리
3. Pulse 업로드 후 분석 리포트 버전 생성
4. 비공개 포뮬라 엔진 계약 정의
5. 문화 확산 맵 결과 객체 생성
6. 대시보드 위젯 추가/삭제 구조
7. 세션 기획 보드와 피드백 입력
8. 메시지 히스토리 저장

MVP에서 Cloud Function까지 바로 구현하지 못하더라도, 로컬 임시 엔진과 서버 사이드 전환 경로는 처음부터 분리해 둔다.

---

## 15. 최종 개발 산출물

최종적으로 다음 산출물을 만든다.

- 개발 요건 기획서
- 데이터 모델 명세
- 업로드 템플릿
- 조직명 매칭 UX
- Pulse 문항 관리 페이지
- 분석 리포트 엔진
- 비공개 포뮬라 엔진 계약
- 선택형 대시보드
- Pulse Story 페이지
- 세션 보드
- 메시지 보드
- 세션 피드백 업로드
- 문화 확산 맵
- 연도별 비교 리포트
- 검증 루프 리포트

---

## 16. 다음 작업 체크리스트

다음 개발을 시작하기 전 먼저 할 일:

- [ ] 현재 앱 파일 구조 확인
- [ ] 현재 저장 데이터 구조 확인
- [ ] 조직도 데이터 최신본 확인
- [ ] Pulse 문항 세트 현재 구조 확인
- [ ] 조직명 매칭 UI 와이어프레임 작성
- [ ] Pulse 문항 관리 페이지 와이어프레임 작성
- [ ] 분석 리포트 객체 스키마 작성
- [ ] 비공개 포뮬라 엔진 입출력 계약 작성
- [ ] 대시보드 위젯 라이브러리 1차 목록 확정
- [ ] 세션 설문 문항 1차 세트 확정
- [ ] 세션/메시지 보드 데이터 모델 확정

---

## 17. 한 줄 결론

다음 버전의 Lina Culture Platform은 `Pulse Survey를 보여주는 도구`가 아니라, `조직 변화 신호를 감지하고, 실행을 설계하고, 피로도를 낮추며, 신뢰와 변화 유연성을 검증하는 조직문화 운영 엔진`으로 개발한다.
