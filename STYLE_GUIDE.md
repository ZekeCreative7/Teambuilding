# Lina Culture Platform Style Guide

## 1. 디자인 방향

Lina Culture Platform은 조직도와 문화 신호를 한눈에 읽는 운영형 플랫폼이다. 첫 화면은 항상 조직도가 중심이어야 하며, 보조 정보는 차트의 흐름을 방해하지 않는 밀도와 위계로 배치한다.

- 핵심 인상: 가볍고 밝지만, 임원 보고 화면처럼 정돈된 느낌
- 우선순위: 조직도 읽기 > 선택 조직 이해 > 설정/편집
- 편집 원칙: 읽는 화면은 단순하게, 수정은 명확한 버튼 이후에 열기

## 2. 색상

| 역할 | 값 | 사용처 |
| --- | --- | --- |
| Primary | `#5f6dee` | 주요 버튼, 선택 상태, 조직도 강조 |
| Secondary | `#18b6aa` | 확산/안정 신호, 보조 액션 |
| Warm Accent | `#ffd166` | 카운트 배지, 긍정 보조 강조 |
| Risk | `#fb7185` | 리스크, 피로도, 지원 필요 |
| Text Strong | `#0b1020` | 제목, 핵심 수치 |
| Text Muted | `#64748b` | 설명, 보조 메타 |
| Surface | `rgba(255, 255, 255, 0.86)` | 카드, 패널, 캔버스 |
| Line | `rgba(203, 213, 225, 0.72)` | 구분선, 조직도 연결선 |

색을 넓게 채울 때는 단색보다 `Primary -> Secondary` 계열의 은은한 그라데이션을 우선 사용한다. 리스크는 넓은 배경보다 작은 칩, 점, 바에서 사용한다.

## 3. 레이아웃

- 좌측 메뉴는 어두운 네비게이션으로 유지하고, 본문은 밝은 유리감의 업무 화면으로 구성한다.
- People & Organization에서는 조직도가 기본 탭이며, 요약 지표는 조직도 프레임 안에서 작게 보조한다.
- 조직 카드를 선택하면 조직도는 왼쪽에 남고, 오른쪽에 선택 조직 요약이 같은 높이로 열린다.
- 설정은 오른쪽 슬라이딩 패널에서만 수행한다.

## 4. 조직도 카드

- 카드 폭은 조직명이 잘리지 않도록 충분히 확보한다.
- 카드 상단은 조직 유형, 조직명, 책임자 이름, 직급/호칭 순서로 읽힌다.
- 책임자 호칭은 고정 단어가 아니라 설정값이다. 예: 대표이사, 부문장, 본부장, 팀장, 파트장.
- 하위 조직 버튼은 검정색을 쓰지 않고 `Primary -> Secondary` 그라데이션 원형 버튼을 사용한다.
- 상세 값은 카드가 열렸을 때만 표시한다. 기본 상태에서는 조직명과 책임자 정보가 먼저 보인다.

## 5. 패널과 버튼

- 주요 버튼: 선명한 그라데이션, 흰색 텍스트, 부드러운 그림자
- 보조 버튼: 흰색 또는 투명 배경, 얇은 라인
- 위험 버튼: 붉은 계열을 작게 사용하고, 넓은 면적을 차지하지 않는다.
- 패널은 `28px-32px` 라운드, 얇은 흰색 라인, 은은한 그림자를 기본으로 한다.
- 카드 안에 또 다른 큰 카드를 중첩하지 않는다.

## 6. 데이터 표현

- 변화 수용도(Change Readiness), 신뢰(Trust), 피로도(Fatigue)는 한글을 먼저 쓰고 영문은 괄호로 보조한다.
- 본부 이상은 Pulse Survey 기반 자동 분석값을 우선 사용한다.
- 팀 단위는 설정 패널에서 직접 조정할 수 있게 둔다.
- 문화 확산 맵의 점은 작게 유지하고, 레벨 버튼(전사/부문/본부/팀)으로 복잡도를 조절한다.

## 7. 문구 원칙

- 조직 역할은 “리더”로 고정하지 않는다.
- 화면에서는 “책임자”, “본부장”, “부문장”, “팀장”, “파트장”처럼 실제 운영 언어를 사용한다.
- 사용법 설명을 길게 쓰기보다 버튼명과 정보 위계로 다음 행동이 보이게 만든다.
- 제품명은 `Lina Culture Platform`으로 통일한다.
